#include <vector>
#include <stdio.h>
#include <iostream>
#include <queue>
#include "Graph.h"
#include "exeption.h"

using namespace std;

//void sort(queue<int>& q)
//{
//	int tmp = Q.
//}

int main()
{
	priority_queue<Vertex> Q;
	Q.push(Vertex(30));
	Q.push(Vertex(7));
	Q.push(Vertex(5));
	Q.push(Vertex(6));

	//while (!Q.empty())  // пока очередь не станет пустой
	//{
	//	cout << Q.top().id_v << endl;
	//	Q.pop();    // извлекаем первый элемент
	//}
}